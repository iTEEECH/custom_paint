name: smiley
description: Example of smiley.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  # https://pub.dev/packages/flutter_lints
  flutter_lints: 6.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
